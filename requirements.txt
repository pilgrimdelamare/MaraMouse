# mediapipe DEVE essere <0.10.20: dalle release recenti (es. 0.10.35) Google ha
# rimosso le legacy mp.solutions.hands, lasciando solo la Tasks API che su CPU
# non rileva la mano in modo affidabile. La 0.10.14 ha ancora mp.solutions.hands.
mediapipe==0.10.14
opencv-python>=4.8.0
pynput>=1.7.6
numpy>=1.24.0
# --- Audio gate (wake word + speaker verification) ---
sounddevice>=0.4.6
soundcard>=0.4.0
vosk>=0.3.45
# openwakeword e' opzionale: se presente un modello ONNX custom in models/,
# viene usato al posto di Vosk. Altrimenti Vosk basta da solo.
# openwakeword>=0.6.0
# speechbrain trascina torch e torchaudio (~2GB). Speaker verification e'
# opzionale: senza speechbrain il gate funziona solo con la wake word.
# speechbrain>=1.0.0
